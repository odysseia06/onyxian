# The canonical example persona: a researcher who is
# also a working developer. Presets mirror the maintainer's proven naming
# (owner-confirmed): research nests under the academic root, and the AI
# workspace keeps the canonical vault's General/ shelves.
name: researcher-developer
modules: [core, daily-notes, academic, research, reading, projects-software, oss, fitness, ai-workspace]
presets:
  daily-notes: { granularity: "YYYY/MM" }
  research: { root: "Academic/Research" }
  ai-workspace: { root: "General" }
