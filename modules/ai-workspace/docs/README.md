# ai-workspace

The smallest module in the roster (KICKSTART.md §5.4): two folders and a dashboard, generalized from the maintainer's `General/Prompts` + `General/Agent-Skills` shelves. Deliberately schema-free — prompt notes and skill drafts vary too much for typed frontmatter to help; the core keys suffice.

Variables: `root` (default `AI-Workspace`; the canonical-example vault keeps these shelves under `General/`, claimable via the variable on adopt). Seeded: `00 Dashboard.md`. No skills, no agents: this module is a place, not a workflow.
