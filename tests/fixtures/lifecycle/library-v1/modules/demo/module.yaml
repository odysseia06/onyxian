name: demo
version: 0.1.0
summary: >
  Synthetic demo module for the lifecycle goldens: a folder, two managed
  templates (one retired in v2), and one seed (changed in v2 but never
  redelivered to existing vaults).
depends: [core]
provides:
  folders:
    - "Demo-Area"
  templates:
    - "Templates/Demo/Guide.md"
    - "Templates/Demo/Old-Asset.md"
seeds:
  - "Start.md"
